#   1. починить кнопку "Назад" в списке годов, чтобы он снимала сразу и индекс страницы, и параметр min/max
    2. починить "вложенные" кнопки (id fix) (Links_fix.py)
    3. search_cards 
        # 3.1 проверить не обнуляются ли идентификаторы при удалении сообщений -> нет, не обнуляются. пользователь не может удалить переписку для бота
        3.2 создавать "карточку" по команде "/start"
                                    -> не старт, а новый поиск  ## хня какая-то ##, значит надо по айди хранить гибкую структуру: карточку или что-то ещё. или вынести формирование карточки в отдельную команду
        3.3 организовать запись параметров в карточку 
        3.4 организовать запись года в карточку

    4. ну 600 строк без нормальной структуры чёт так себе варик
    5. срастить c GitHub
!   6. В userClass "subscriptions" по сути идентичен "search_cards" ?
    7. подогнать всё к subscription_example из userClass (ало там готовое решение дурачок)

#!b   8. нажатие кнопки "назад" ведёт к отправке к нового сообщения  
    9.0. delete userClass.py
    9.1. complete avassist_types.py

    10. сейчас пользователем выступае чат, что создаёт потенциал для фарма/ддоса сёрчей. Возможное решение: при создании карточки, проверять не превысил ли юзер(не пользователь, а аккаунт) свой лимит 
    11. User.check_permission